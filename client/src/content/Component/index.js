import React from 'react';
import ReactDOM from 'react-dom/client';
import ContentArchive from './ContentArchive.js';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ContentArchive />
  </React.StrictMode>
);
